# TrueFare runs in guest mode (all data in this browser) when these are absent.
# Point them at a Supabase project and apply supabase/migrations to enable
# accounts, cross-device history, and cross-user trending.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
